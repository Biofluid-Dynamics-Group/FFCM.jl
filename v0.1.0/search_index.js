var documenterSearchIndex = {"docs":
[{"category":"section","location":"method/overview/#The-mobility-operator","page":"The mobility operator","text":"ForceCouplingMethod.jl evaluates the mobility operator mathcalM^mathcalVmathcalF of Su & Keaveny (2024): the linear map taking the forces mathcalF = (boldsymbolF_n)_n=1^N on N unit-radius spheres at positions mathcalY = (boldsymbolY_n)_n=1^N to the velocities mathcalV = (boldsymbolV_n)_n=1^N they induce through the triply-periodic Stokes flow (§2, equations (5)–(6)). In the force-coupling method each point force is regularised by a Gaussian kernel whose width sigma = asqrtpi is set by the particle radius a, chosen so that a single sphere recovers the Stokes drag law (§2).","title":"The mobility operator"},{"category":"section","location":"method/overview/#The-fast-FCM-splitting","page":"The mobility operator","text":"Resolving the width-sigma kernel directly ties the grid resolution of the Stokes solver to the particle radius. Fast FCM decouples the two through the splitting\n\nmathcalM^mathcalVmathcalF\n= tildemathcalM^mathcalVmathcalF\n+ left(mathcalM^mathcalVmathcalF - tildemathcalM^mathcalVmathcalFright)\nqquad text(3 equation (19))\n\nThe smooth part tildemathcalM^mathcalVmathcalF is the FCM mobility of a wider kernel, of width Sigma geq sigma, which the spectral steps — spreading, the Stokes solve, and interpolation — resolve on a correspondingly coarser grid. The remainder mathcalM^mathcalVmathcalF - tildemathcalM^mathcalVmathcalF decays like a Gaussian in the pair separation, so it is applied as a sparse real-space pairwise sum over neighbours within a cutoff R_c. The corrected result recovers the sigma-regularised mobility independent of Sigma: the ratio Sigmasigma and the cutoff R_c become tuning parameters that trade grid work against pair work (§5).","title":"The fast-FCM splitting"},{"category":"section","location":"method/overview/#The-six-step-pipeline","page":"The mobility operator","text":"mobility!(V, config, Y, F) composes the six sub-algorithms of §4, applied in order. Step 1 (spatial hashing) is two calls — the position wrap and the cell hash — so the six steps are seven calls:\n\nwrap_positions!(config.particles.Y_wrapped, Y, config.L)        # step 1: fold into [0, L)\nassign_cells!(config, config.particles.Y_wrapped)               # step 1: cell hash\nsort_particles_by_cell!(config, config.particles.Y_wrapped, F)  # step 2: cell list + gather\nspread_forces!(config)                                # step 3: J̃†  (forces → density)\nstokes_solve!(config)                                 # step 4: L⁻¹ (FFT Stokes solve)\ninterpolate_velocities!(V, config)                    # step 5: J̃   (velocity → particles)\ncorrect_velocities!(V, config)                        # step 6: + (M − M̃) real-space\n\nSteps 1–2 build the cell list and gather the forces into cell order; steps 3–5 compute tildemathcalM^mathcalVmathcalFmathcalF (the spread, the Stokes solve, and the interpolation); step 6 adds the real-space remainder (the pairwise correction). Interpolation and the correction both write V in the caller's original particle order — interpolation scatters back through the step-2 permutation, and the correction adds onto the same buffer — so the composition needs no final reordering.","title":"The six-step pipeline"},{"category":"section","location":"method/overview/#Symmetric-positive-definiteness","page":"The mobility operator","text":"The assembled mobility is symmetric positive-definite — the property a conjugate-gradient resistance solve depends on (§3.3, \"Positive splitting\"). Writing S for the spread matrix (entry S_gn = tildeDelta_n(boldsymbolx_g Sigma), the weight particle n contributes to grid point g), P for the step-2 sort permutation, and mathcalL^-1 for the Stokes solve, the smooth part assembles as\n\ntildemathcalM^mathcalVmathcalF = h^3 (SP)^T mathcalL^-1 (SP)\n\nmathcalL^-1 is symmetric positive-semidefinite: per Fourier mode it is the orthogonal projector boldsymbolI - hatboldsymbolkhatboldsymbolk^T scaled by the positive factor 1(mu k^2). Interpolation being the exact discrete adjoint of spreading — the uniform h^3 quadrature weight, see why the uniform h³ weight — makes the sandwich symmetric positive-semidefinite for any S, and positive-definite for distinct-particle configurations. The real-space correction is a symmetric pair tensor that vanishes at Sigma = sigma (see the pairwise correction), so the splitting preserves the structure.  The FFCMMobility operator declares issymmetric and isposdef accordingly.","title":"Symmetric positive-definiteness"},{"category":"section","location":"method/overview/#The-matrix-free-interface","page":"The mobility operator","text":"Iterative solvers see flat vectors; the pipeline works on 3 times N matrices. The convention bridging them is the column-major vec of a 3 times N matrix: entry 3(n-1) + i holds axis i in 1 2 3 of particle n, so a flat velocity vector is equivalently reshape(v, 3, N).\n\nFFCMMobility(config, Y) closes over a configuration and a fixed position matrix and implements the LinearAlgebra operator interface: size(M) == (3N, 3N), allocation-free three- and five-argument mul!, and the allocating convenience M * F in the natural 3 times N layout. The operator drops into IterativeSolvers.gmres!, KrylovKit.linsolve, or any mul!-based solver without a call-site adapter.","title":"The matrix-free interface"},{"category":"section","location":"method/spreading-interpolation/#Spreading-and-interpolation","page":"Spreading and interpolation","text":"Steps 3 and 5 of the pipeline (Su & Keaveny 2024, §4) are a transpose pair: spreading scatters the particle forces onto the spectral grid as a smooth force density, and interpolation gathers the solved fluid velocity back to the particles. Both use the same modified Gaussian kernel over the same per-particle stencil, and that sharing is not a convenience but a structural requirement — it is what makes the assembled mobility symmetric (see below).","title":"Spreading and interpolation"},{"category":"section","location":"method/spreading-interpolation/#The-FCM-kernels","page":"Spreading and interpolation","text":"The original FCM kernel is the isotropic Gaussian centred on particle n,\n\nDelta_n(boldsymbolx sigma)\n= frac1(2pisigma^2)^32\n  e^-lvert boldsymbolx - boldsymbolY_n rvert^2  2sigma^2\nqquad (text2 equation (1))\n\nwith the width sigma = asqrtpi fixed by the particle radius so that a single sphere recovers the Stokes drag law (§2). All lengths in the package are expressed in units of a, so a = 1. Fast FCM spreads with the modified kernel at the wider width Sigma geq sigma,\n\ntildeDelta_n(boldsymbolx Sigma)\n= left(1 + fracsigma^2 - Sigma^22 Deltaright) Delta_n(boldsymbolx Sigma)\nqquad (text3 equation (22))\n\nwhere Delta denotes the Laplacian. A wider kernel reduces the grid resolution the Stokes solver needs; the hydrodynamic error this introduces decays like a Gaussian in the pair separation and is removed by the pairwise correction. Evaluating the Laplacian of the Gaussian gives the closed form the code uses,\n\ntildeDelta_n(boldsymbolx Sigma) = left(a_0 + a_2 r_n^2right)\nDelta_n(boldsymbolx Sigma)\nqquad\na_0 = 1 - frac3(sigma^2 - Sigma^2)2Sigma^2\nquad\na_2 = fracsigma^2 - Sigma^22Sigma^4\n\nwith r_n = lvert boldsymbolx - boldsymbolY_n rvert. At Sigma = sigma the coefficients collapse to a_0 = 1, a_2 = 0 and the method reduces to standard FCM — supported because it is free and useful for validation.","title":"The FCM kernels"},{"category":"section","location":"method/spreading-interpolation/#Spreading-forces-to-the-grid","page":"Spreading and interpolation","text":"The spreading operator maps the N forces to a smooth force density,\n\ntildemathcalJ^daggermathcalF(boldsymbolx)\n= sum_n=1^N boldsymbolF_n tildeDelta_n(boldsymbolx Sigma)\nqquad (text3 equation (25))\n\nSampled on the grid, each particle contributes only to the M_G^3 grid points of its stencil — the Gaussian decays so fast that the truncation error is exponentially small in M_G (the (M_G Sigmah) accuracy calibration is §5, Table 1 and Fig. 1(a)). Because the kernel integrates to one (the Gaussian integrates to one and the Laplacian term integrates to zero), the spread field conserves the total force and its first moment up to that truncation.","title":"Spreading forces to the grid"},{"category":"section","location":"method/spreading-interpolation/#Separability","page":"Spreading and interpolation","text":"The isotropic Gaussian factors per axis: with g(s Sigma) = (2piSigma^2)^-12 e^-s^22Sigma^2,\n\nDelta_n(boldsymbolx_g Sigma)\n= g(x_i_x - Y_n1) g(y_i_y - Y_n2) g(z_i_z - Y_n3)\nqquad\nr_n^2 = sum_i (x_i - Y_ni)^2 \n\nPer particle, the kernel therefore precomputes per-axis one-dimensional Gaussian weights and squared distances — mathcalO(M_G) exponentials per axis — and the inner M_G^3 loop assembles tildeDelta_n from three multiplies and the polynomial factor (a_0 + a_2 r_n^2), evaluating no further exponentials.","title":"Separability"},{"category":"section","location":"method/spreading-interpolation/#Stencil-anchoring","page":"Spreading and interpolation","text":"For axis i and particle n the stencil is anchored at the nearest grid point, j_i = operatornameround(Y_nih) with ties rounding to even, and covers the grid indices j_i - lfloor M_G2 rfloor + s for s = 0 dots M_G - 1, wrapped periodically bmod M_i. For odd M_G the stencil is symmetric about the anchor; for even M_G it covers one more point below than above. Nearest-grid-point anchoring is the convention the (M_G Sigmah) calibration of §5 (Table 1) assumes. A particle near the domain edge spreads to a stencil that wraps to the opposite side, identically to an interior particle.","title":"Stencil anchoring"},{"category":"section","location":"method/spreading-interpolation/#Interpolating-velocities-from-the-grid","page":"Spreading and interpolation","text":"The interpolation operator is the kernel-weighted volume average of the fluid velocity,\n\nboldsymbolV_n = tildemathcalJboldsymbolu_n\n= int_Omega boldsymbolu(boldsymbolx) tildeDelta_n(boldsymbolx Sigma)\n mathrmdboldsymbolx\nqquad (text3 equation (26))\n\nWith boldsymbolu known at the grid points, the integral is approximated by the trapezoidal rule with the uniform weight h^3 over the same M_G^3 stencil as the spread:\n\nboldsymbolV_n = h^3 sum_textstencil\nboldsymbolu(boldsymbolx_g) left(a_0 + a_2 r_n^2right) g_x g_y g_z \n\nThe only differences from the spread are the direction — a gather from the grid rather than a scatter to it — and the quadrature factor h^3, applied once per particle. The result is written in the caller's original particle order (the step-2 sort permutation is folded into the output write).","title":"Interpolating velocities from the grid"},{"category":"section","location":"method/spreading-interpolation/#Why-the-uniform-h-weight","page":"Spreading and interpolation","text":"Two independent reasons fix both the quadrature rule and its weight; neither is a free choice.\n\nExact discrete adjointness makes the mobility symmetric. Let S be the spread matrix, S_gn = tildeDelta_n(boldsymbolx_g Sigma). The trapezoidal interpolation above is exactly the matrix h^3 S^T acting on the grid velocity, so the assembled smooth mobility is the symmetric sandwich h^3 (SP)^T mathcalL^-1 (SP) described in the overview. This holds only because interpolation is the exact transpose of spreading: a non-uniform quadrature weight would replace h^3 by a diagonal W neq h^3boldsymbolI, and Gauss-type nodes would change the node set entirely — either breaks the transpose relation and with it the symmetry a conjugate-gradient resistance solver depends on. In the implementation the spread and the interpolation share one stencil-fill routine, so the interpolation weights are bit-for-bit the spread weights and the adjointness is exact in floating point, not just in exact arithmetic.\n\nThe trapezoidal rule is spectrally accurate here. On a periodic domain the trapezoidal rule converges faster than any power of h for smooth integrands. The Euler–Maclaurin formula writes its error as a series of boundary terms in the odd derivatives at the interval ends; for an L-periodic integrand every derivative matches at the endpoints and every term cancels. The integrand here — the product of the discrete velocity field (a trigonometric polynomial) and the periodised Gaussian kernel — is C^infty and periodic, so the full-grid trapezoidal sum is spectrally accurate, and restricting it to the M_G^3 stencil drops only the exponentially small Gaussian tail.","title":"Why the uniform h³ weight"},{"category":"section","location":"performance/#Performance-tips","page":"Performance tips","text":"The package follows a two-phase design: construction (FFCMConfig) may be slow and allocate freely; the operator application (mobility! and the FFCMMobility mul! methods) is allocation-free and meant to run many times — once per iteration of a downstream iterative solver. The knobs below tune that trade.","title":"Performance tips"},{"category":"section","location":"performance/#Warm-up-before-timing","page":"Performance tips","text":"The first mobility! (or M * F) call after loading the package incurs Julia's just-in-time compilation. For timing or long solves, make one warmup call on the configuration first; subsequent calls run at full speed.","title":"Warm up before timing"},{"category":"section","location":"performance/#FFT-planner-effort-(fft_planning)","page":"Performance tips","text":"The fft_planning keyword (:estimate, :measure — the default — or :patient) selects the FFTW planner effort. Planner effort trades construction time for transform time: :estimate plans immediately from heuristics, while :measure and :patient time candidate algorithms on the actual grid at construction and can produce faster plans for the many Stokes solves of a long resistance solve. Measured on 64³–128³ grids, :measure plans ran the Stokes solve 9–22 % faster than :estimate for a one-time planning cost of roughly 0.3–1.3 s. Choose :estimate when construction latency matters more than per-call speed — small grids, one-shot evaluations. The solution is independent of the planner effort to round-off.","title":"FFT planner effort (fft_planning)"},{"category":"section","location":"performance/#FFTW-wisdom","page":"Performance tips","text":"FFTW can cache planner results across sessions through its wisdom mechanism, with no ForceCouplingMethod involvement: call FFTW.import_wisdom(path) before constructing the configuration and FFTW.export_wisdom(path) after. A wisdom-loaded construction gets measured-quality plans at :estimate-like planning cost.","title":"FFTW wisdom"},{"category":"section","location":"performance/#FFT-threading-(fft_threads)","page":"Performance tips","text":"The fft_threads keyword (default 1) bakes a thread count into the two FFT plans. Threaded transforms help on large grids — on an 8-thread machine the Stokes solve ran 23–44 % faster at 64³/128³ — but were slower at 32³, so opt in for large grids only. Two caveats: threaded FFTW plans execute as spawned Julia tasks, which allocate on every call, so the allocation-free hot-path guarantee is scoped to fft_threads = 1; and the thread count is per-plan state, so it leaves no global FFTW state behind. The solution matches the single-threaded result to round-off.","title":"FFT threading (fft_threads)"},{"category":"section","location":"performance/#The-cell-list-is-rebuilt-every-call","page":"Performance tips","text":"mobility! rebuilds the cell list (wrap → hash → sort → gather) on every call, even when the same positions are reused across a linear solve. This keeps the public contract a single call and is cheap: the cell-list build is mathcalO(N), dominated by the spread and interpolation (mathcalO(N M_G^3)) and the FFT (mathcalO(M log M)) that must run every call because the forces change.","title":"The cell list is rebuilt every call"},{"category":"section","location":"performance/#GPU","page":"Performance tips","text":"For large problems the CUDA backend is the main lever: construct with gpu_acceleration = true and use Float32. The same warmup advice applies — the first call additionally JIT-compiles the device kernels.","title":"GPU"},{"category":"section","location":"method/pairwise-correction/#The-pairwise-correction","page":"The pairwise correction","text":"Step 6 of the pipeline (Su & Keaveny 2024, §4) removes the error the wider spreading kernel introduced. The splitting mathcalM^mathcalVmathcalF = tildemathcalM^mathcalVmathcalF + (mathcalM^mathcalVmathcalF - tildemathcalM^mathcalVmathcalF) leaves a difference operator that decays like a Gaussian in the pair separation, so it is applied as a sparse pairwise sum over neighbours within the cutoff R_c, found through the cell list.","title":"The pairwise correction"},{"category":"section","location":"method/pairwise-correction/#A-difference-of-two-FCM-mobilities","page":"The pairwise correction","text":"The FCM pairwise mobility relating the force on particle m to the velocity of particle n is the regularised Stokeslet at the sqrt2-scaled width,\n\nboldsymbolM^mathcalVmathcalF_nm\n= boldsymbolS(boldsymbolY_n - boldsymbolY_m sigmasqrt2)\nqquad (text2 equation (14))\n\nand the modified-kernel mobility expands as\n\ntildeboldsymbolM^mathcalVmathcalF_nm\n= boldsymbolS(Sigmasqrt2)\n+ (sigma^2 - Sigma^2)boldsymbolQ(Sigmasqrt2)\n+ frac(sigma^2 - Sigma^2)^24boldsymbolT(Sigmasqrt2)\nqquad (text3 equation (29))\n\nwith boldsymbolS = boldsymbolS^(1) + boldsymbolS^(2) + boldsymbolS^(3) (§2, equations (8)–(10)), boldsymbolQ = boldsymbolQ^(1) + boldsymbolQ^(2) (§2, equations (16)–(17)), and boldsymbolT (§3, equation (30)). Their difference is the correction. The package evaluates the paper's regrouped closed form (§3, equation (31)) directly: the (boldsymbolG + sigma^2DeltaboldsymbolG) grouping — with boldsymbolG the Stokeslet (Oseen tensor, §2, equation (11)) and DeltaboldsymbolG its Laplacian — analytically pre-cancels the operatornameerf of boldsymbolQ^(1), so only boldsymbolQ^(2) and boldsymbolT (both Gaussian-only) survive as explicit terms.","title":"A difference of two FCM mobilities"},{"category":"section","location":"method/pairwise-correction/#The-two-scalar-collapse","page":"The pairwise correction","text":"Write boldsymbolx = boldsymbolY_n - boldsymbolY_m, r = lVertboldsymbolxrVert, and the sqrt2-scaled Gaussian Delta_w equiv Delta(boldsymbolx wsqrt2) = (4pi w^2)^-32 e^-r^24w^2. Every constituent block is isotropic in the same two tensors, boldsymbolI and the un-normalised boldsymbolx otimes boldsymbolx:\n\nBlock coefficient of boldsymbolI coefficient of boldsymbolx otimes boldsymbolx\nboldsymbolG dfrac18pimu r dfrac18pimu r^3\nsigma^2DeltaboldsymbolG dfracsigma^24pimu r^3 -dfrac3sigma^24pimu r^5\nboldsymbolS^(3)(wsqrt2) -dfrac2w^4mu r^2Delta_w +dfrac6w^4mu r^4Delta_w\nboldsymbolQ^(2)(Sigmasqrt2) -dfrac1muleft(1+dfrac2Sigma^2r^2right)Delta_Sigma +dfrac1mu r^2left(1+dfrac6Sigma^2r^2right)Delta_Sigma\nboldsymbolT(Sigmasqrt2) dfrac12muSigma^2left(2-dfracr^22Sigma^2right)Delta_Sigma dfrac14muSigma^4Delta_Sigma\n\nso the whole correction collapses to two pair scalars,\n\nboldsymbolM_nm - tildeboldsymbolM_nm\n= A(r)boldsymbolI + B(r)boldsymbolx otimes boldsymbolx\n\nWriting mathrmerf_2w = operatornameerf(r2w),\n\nA(r) = (mathrmerf_2sigma-mathrmerf_2Sigma)\nleft(frac18pimu r+fracsigma^24pimu r^3right)\n- frac2sigma^4mu r^2Delta_sigma\n+ leftfrac2Sigma^4mu r^2\n+ fracsigma^2-Sigma^2muleft(1+frac2Sigma^2r^2right)\n- frac(sigma^2-Sigma^2)^24cdotfrac2-r^22Sigma^22muSigma^2right\nDelta_Sigma \n\nB(r) = (mathrmerf_2sigma-mathrmerf_2Sigma)\nleft(frac18pimu r^3-frac3sigma^24pimu r^5right)\n+ frac6sigma^4mu r^4Delta_sigma\n+ left-frac6Sigma^4mu r^4\n- fracsigma^2-Sigma^2mu r^2left(1+frac6Sigma^2r^2right)\n- frac(sigma^2-Sigma^2)^24cdotfrac14muSigma^4right\nDelta_Sigma \n\nThe velocity correction to particle n from the force on a neighbour m is then\n\nDeltaboldsymbolV_n mathrel+= A(r)boldsymbolF_m\n+ B(r)(boldsymbolxcdotboldsymbolF_m)boldsymbolx \n\nCollapsing to the two scalars replaces a dense 3times3 matrix–vector product with two scalar evaluations, and the Sigmasqrt2 operatornameerf and Gaussian are shared across the boldsymbolQ^(2) and boldsymbolT terms, so only two operatornameerf and two exp are evaluated per interaction.","title":"The two-scalar collapse"},{"category":"section","location":"method/pairwise-correction/#The-erf-argument-in-equation-(31)","page":"The pairwise correction","text":"The printed equation (31) shows the factor operatornameerf(rsigmasqrt2) - operatornameerf(rSigmasqrt2), but the correct argument is operatornameerf(r2sigma) - operatornameerf(r2Sigma): the printed equation carries a typo in the operatornameerf argument. To see it, the regularised Stokeslet is boldsymbolS^(1)(boldsymbolx sigma) = operatornameerf(rsigmasqrt2)boldsymbolG (equation (12)), and the pairwise mobility evaluates it at the sqrt2-scaled width (equation (14)), so boldsymbolS^(1) inside boldsymbolM^mathcalVmathcalF carries operatornameerf(r2sigma). Collecting the DeltaboldsymbolG terms from ``\\boldsymbol{S}^{(2)}(\\sigma\\sqrt{2}) - \\boldsymbol{S}^{(2)}(\\Sigma\\sqrt{2})\n\n(\\sigma^2-\\Sigma^2)\\boldsymbol{Q}^{(1)}(\\Sigma\\sqrt{2})`` then yields the coefficient\n\nsigma^2(mathrmerf_2sigma - mathrmerf_2Sigma), fixing the argument as r2w. The package implements r2w; the test suite pins it against an algebraically independent oracle (the difference of the full mobility expansions), which would fail under the printed argument.","title":"The erf argument in equation (31)"},{"category":"section","location":"method/pairwise-correction/#The-self-term","page":"The pairwise correction","text":"At r = 0 the boldsymbolx otimes boldsymbolx part vanishes and the correction has the well-defined diagonal limit deltaboldsymbolI with\n\ndelta = frac16pimu a - frac16pimu(Sigmasqrtpi)\n+ fracsigma^2-Sigma^212mu(Sigmasqrtpi)^3\n- frac(sigma^2-Sigma^2)^232muSigma^5pi^32 \n\nadded to every particle independent of its neighbours (boldsymbolV_n mathrel+= deltaboldsymbolF_n). It is what makes the single-sphere self-mobility come out Sigma-independent end to end.","title":"The self term"},{"category":"section","location":"method/pairwise-correction/#Symmetry,-the-Σ-σ-limit,-and-periodicity","page":"The pairwise correction","text":"Because A and B depend only on r and AboldsymbolI + Bboldsymbolx otimes boldsymbolx is a symmetric tensor, the pair correction is self-adjoint, preserving the symmetric positive-definite structure of the mobility. At Sigma = sigma every Gaussian term and the operatornameerf difference vanish and delta = 0: the correction is identically zero, consistent with tildemathcalM^mathcalVmathcalF = mathcalM^mathcalVmathcalF there.\n\nUnder periodicity each pair interacts through its nearest image only: the separation is reduced to -L_i2 L_i2 per axis by x_i - L_ioperatornameround(x_iL_i) (round-to-nearest-ties-to-even, which makes the reduction exactly antisymmetric and the pair correction exactly symmetric), and the construction-time precondition R_c leq min(L)2 guarantees a single relevant image and excludes any particle correcting against its own periodic copy.","title":"Symmetry, the Σ = σ limit, and periodicity"},{"category":"section","location":"method/stokes-solve/#The-spectral-Stokes-solve","page":"The spectral Stokes solve","text":"Step 4 of the pipeline (Su & Keaveny 2024, §4) solves the periodic Stokes problem driven by the spread force density boldsymbolf = tildemathcalJ^daggermathcalF,\n\n-mu Delta boldsymbolu + nabla p = boldsymbolf\nqquad\noperatornamediv(boldsymbolu) = 0\nqquad textin  Omega\nqquad (text3 equations (23)(24))\n\nwith triply-periodic boundary conditions, by a Fourier spectral method: transform the force density, invert the Stokes operator mode by mode, and transform back.","title":"The spectral Stokes solve"},{"category":"section","location":"method/stokes-solve/#Fourier-space-inversion","page":"The spectral Stokes solve","text":"The Fourier transform turns the differential operators into multiplications. Writing hatboldsymbolu, hatp, hatboldsymbolf for the transforms and k = lvertboldsymbolkrvert,\n\nmu k^2 hatboldsymbolu + i boldsymbolk hatp = hatboldsymbolf\nqquad\ni boldsymbolk cdot hatboldsymbolu = 0 \n\nDotting the momentum equation with boldsymbolk and using incompressibility eliminates the velocity and gives the pressure, hatp = -i (boldsymbolk cdot hatboldsymbolf)k^2. Substituting back yields the projector form\n\nhatboldsymbolu(boldsymbolk)\n= frac1mu k^2\nleft(boldsymbolI - fracboldsymbolkotimesboldsymbolkk^2right)\nhatboldsymbolf(boldsymbolk)\nqquad boldsymbolk neq boldsymbol0\n\neach mode is projected onto the divergence-free subspace and scaled by 1(mu k^2).","title":"Fourier-space inversion"},{"category":"section","location":"method/stokes-solve/#The-mean-flow-gauge-fix","page":"The spectral Stokes solve","text":"At boldsymbolk = boldsymbol0 the momentum equation reduces to boldsymbol0 = hatboldsymbolf(boldsymbol0): the solvability condition is that the total integrated force vanishes, and when it does, the mean velocity is undetermined — the solution is fixed only up to a uniform translation. The standard gauge choice sets hatboldsymbolu(boldsymbol0) = boldsymbol0, the frame in which the volume-averaged fluid velocity vanishes. For a physical mobility problem with sum_n boldsymbolF_n = boldsymbol0 the discarded mean is zero up to the stencil truncation of the spread; any residual mean is absorbed by the gauge fix and does not affect any non-zero mode.","title":"The mean-flow gauge fix"},{"category":"section","location":"method/stokes-solve/#Discrete-transforms-and-normalisation","page":"The spectral Stokes solve","text":"The implementation uses real-to-complex (r2c) forward and complex-to-real (c2r) backward transforms, whose composition is unnormalised: a round trip multiplies by the total grid-point count M = M_x M_y M_z. That factor is folded into the per-mode scalar, so the projection applied in place on the spectrum is\n\nhatboldsymbolu(boldsymbolk) leftarrow\nfrac1mu k^2 M\nleft(hatboldsymbolf(boldsymbolk)\n- fracboldsymbolk(boldsymbolkcdothatboldsymbolf(boldsymbolk))k^2right)\nqquad\nhatboldsymbolu(boldsymbol0) = boldsymbol0","title":"Discrete transforms and normalisation"},{"category":"section","location":"method/stokes-solve/#Wavenumber-layout","page":"The spectral Stokes solve","text":"The r2c transform stores only the non-negative half of the leading axis, so the spectrum has shape (M_x2 + 1 M_y M_z). The per-axis wavenumbers are\n\nk_xi_x = frac2piL_x(i_x - 1)\nqquad\nk_yi_y = frac2piL_y\nbegincases i_y - 1  i_y leq M_y2 + 1  i_y - 1 - M_y  textotherwise endcases\n\nand k_z analogous to k_y — the FFT wrap-around convention that places the negative frequencies in the upper half of each full axis. The zero mode sits at the leading index, where the solve applies the gauge fix as a guarded write of zero (no 10 is ever formed).\n\nThe discrete operator is exact in the trigonometric-polynomial basis the FFT represents: the map from the force density to the fluid velocity is the discrete triply-periodic Stokes solution at the chosen grid resolution, and the solved velocity field is what the interpolation step averages back to the particles.","title":"Wavenumber layout"},{"category":"section","location":"api/#API-reference","page":"API reference","text":"The public surface is three names: a configuration constructor (the cold path, run once), the mutating operator application (the hot path, run per solver iteration), and a matrix-free operator wrapper for iterative solvers. The method overview explains how they fit together; internal step functions are documented in the architecture page of the developer documentation.","title":"API reference"},{"category":"type","location":"api/#ForceCouplingMethod.FFCMConfig","page":"API reference","text":"FFCMConfig{T}(\n    ; L, R_c, N, a = T(1), kernel_widths_ratio, num_grid_points, M_G, viscosity,\n    fft_planning = :measure, fft_threads = 1, gpu_acceleration = false,\n) -> FFCMConfig{T}\nFFCMConfig(; L, ...) -> FFCMConfig{eltype(L)}\n\nConfiguration of the Fast FCM mobility operator: the cell partition of the pairwise correction (paper §4), the FCM grid parameters (paper §3 and §5), and the per-call buffers sized for N particles. Built once and reused across mobility! calls, which write into the buffers this struct owns. N is fixed at construction — changing N requires a new FFCMConfig.\n\nThe compiled buffers are grouped into the cells, particles, grid, solver, and stencil sub-structs; their storage types parameterize FFCMConfig, so selecting the GPU backend is a type swap and the backend never appears on the mobility! hot path.\n\nThe type-parameter-free form infers the working precision T from the element type of the domain lengths L, which must be a concrete subtype of AbstractFloat.\n\nKeywords\n\nL::NTuple{3, T}: the domain lengths (L_x, L_y, L_z) (paper §4). Each component must be positive.\nR_c::T: the cutoff radius of the pairwise correction. Must satisfy 0 < R_c ≤ min(L)/2, so the minimum image is unambiguous and no particle is corrected against its own periodic image (paper §4).\nN::Integer: the number of particles; fixes the hot-path buffer sizes. Must be positive.\na::T = T(1): the particle hydrodynamic radius. Only a == T(1) is currently supported; the kernel width follows σ = a/√π (paper §2, the FCM radius-width relation that recovers the single-particle Stokes drag).\nkernel_widths_ratio::T: the ratio between the wider Gaussian kernel width and the standard FCM kernel width (paper §5). Must satisfy kernel_widths_ratio ≥ T(1); ratio of 1 is the standard FCM limit.\nnum_grid_points::NTuple{3, Int32}: the FFT grid dimensions (M_x, M_y, M_z) (paper §3). The induced spacing h = L_i / M_i must be identical across axes (paper §3 assumption).\nM_G::Integer: the cubic stencil support per axis (paper §5). Stored as Int32; must satisfy 2 ≤ M_G ≤ min(num_grid_points) — the stencil cannot be wider than the grid on any axis, or the periodic wrap would alias distinct stencil points onto the same grid point.\nviscosity::T: the fluid dynamic viscosity (paper §2, Stokes momentum balance). Must be positive.\nfft_planning::Symbol = :measure: the FFT planner effort, one of :estimate, :measure, or :patient. The measuring efforts spend construction time (seconds at large grids) searching for faster transforms for the repeated stokes_solve! calls of a long solve; :estimate plans instantly from heuristics. The computed velocities are independent of the effort to round-off.\nfft_threads::Integer = 1: the execution thread count baked into the FFT plans. Must be at least 1. Threaded plans execute as Julia tasks, which allocate per call: the allocation-free hot path requires fft_threads = 1. The computed velocities match the single-threaded result to round-off. Ignored by the GPU backend.\ngpu_acceleration::Bool = false: build the configuration on a CUDA GPU rather than the CPU. Requires the CUDA extension to be loaded (using CUDA) on a machine with a functional CUDA device; otherwise construction throws. Float64 on the GPU emits a non-fatal warning, since double-precision throughput is a fraction of single-precision on consumer NVIDIA hardware.\n\nReturns\n\nFFCMConfig{T}: the configuration mobility! operates on.\n\nThrows\n\nArgumentError: if R_c ≤ 0, any L_i ≤ 0, R_c > min(L)/2, N ≤ 0, a ≠ T(1) (non-unit radius not yet implemented), kernel_widths_ratio < 1, M_G < 2, M_G > min(num_grid_points), any num_grid_points component < 1, the grid spacing is anisotropic, viscosity ≤ 0, fft_planning is not one of the three planner efforts, fft_threads < 1, or gpu_acceleration = true without the CUDA extension loaded. The type-parameter-free form additionally throws if eltype(L) is not a concrete subtype of AbstractFloat (integer or mixed-precision lengths are rejected, not promoted — the working precision is an explicit choice).\n\n\n\n\n\n","title":"ForceCouplingMethod.FFCMConfig"},{"category":"function","location":"api/#ForceCouplingMethod.mobility!","page":"API reference","text":"mobility!(V, config, Y, F) -> V\n\nApply the Fast FCM mobility operator M^VF (Su & Keaveny 2024, §4): map the N forces F localised at positions Y to the velocities V they induce through the triply-periodic Stokes flow, composing steps 1-6 (spatial hashing, sorting, spreading, the FFT Stokes solve, interpolation, and the real-space correction). The cell list is rebuilt on every call.\n\nArguments\n\nV::AbstractMatrix{T}: a caller-owned 3xN output matrix (column n is particle n, row i is Cartesian axis i); overwritten in the caller's original particle order.\nconfig::FFCMConfig{T}: the compiled configuration owning every hot-path buffer.\nY::AbstractMatrix{T}: the 3xN particle positions. Read-only — folded into [0, L) inside a config-owned buffer, never in the caller's array.\nF::AbstractMatrix{T}: the 3xN applied forces. Read-only.\n\nReturns\n\nV: the same matrix, holding the induced velocities.\n\nThrows\n\nDimensionMismatch: if V, Y, or F is not 3xN for the config's N.\n\n\n\n\n\n","title":"ForceCouplingMethod.mobility!"},{"category":"type","location":"api/#ForceCouplingMethod.FFCMMobility","page":"API reference","text":"FFCMMobility(config, Y) -> FFCMMobility\n\nMatrix-free LinearAlgebra operator backing the mobility action of mobility! for a fixed position matrix Y and a config. It implements size, eltype, and the three- and five-argument mul! (the latter computing v .= α⋅(M⋅f) + β⋅v, with β = 0 overwriting v regardless of its prior contents), so it can be passed to any mul!-based iterative solver. It also declares issymmetric and isposdef (both true; the operator is symmetric positive-definite by construction), so solvers such as IterativeSolvers.cg! dispatch on them.\n\nFor convenience, M * F applies the operator out of place to a force matrix F in the natural 3xN layout, returning a fresh 3xN velocity matrix (the mirror of mobility!). This is distinct from the 3Nx3N flat-vector view reported by size(M) and applied by mul!.\n\nThe operator works on the column-major flat-vector view of the 3xN force/velocity matrices: a length-3N vector with entry 3(n-1)+i holding axis i of particle n. It owns 3xN scratch to marshal between the flat vectors and mobility!'s matrices.\n\nArguments\n\nconfig::FFCMConfig{T}: the compiled configuration; must be built for the same N as Y.\nY::AbstractMatrix{T}: the fixed 3xN particle positions the operator acts at.\n\nReturns\n\nFFCMMobility: the matrix-free operator.\n\nThrows\n\nArgumentError: if Y is not 3xN, or its particle count differs from the config's.\n\n\n\n\n\n","title":"ForceCouplingMethod.FFCMMobility"},{"category":"section","location":"devdocs/architecture/#Architecture","page":"Architecture","text":"This page documents how the implementation is organised: the two-phase API, the buffer ownership boundaries, the dispatch seams that carry the CPU/GPU backend split, and the contracts the hot path is held to. The mathematics of each step lives in the method pages; this page is about the code.","title":"Architecture"},{"category":"section","location":"devdocs/architecture/#Two-phase-design","page":"Architecture","text":"The package is a deep module with two entry points:\n\nCold path — FFCMConfig construction: parameter validation, derived scalars, and the allocation of every per-call buffer and FFT plan. Allocations and ergonomics are fine here; construction may be slow (FFT planning is a deliberate example).\nHot path — mobility! (and the FFCMMobility mul! methods built on it): the single call a downstream iterative solver makes per iteration. It writes only into configuration-owned buffers and the caller's output, allocates nothing, and is fully type-stable.\n\nEvery documented precondition is enforced at the outer boundary — the constructor validates parameters, and mobility! guards its argument sizes — so the kernels stay check-free and their @inbounds annotations are justified by construction-time invariants (for example, the stencil-versus-grid bound M_G leq min(M_x M_y M_z) is what makes the spread's inner-loop writes non-aliasing).","title":"Two-phase design"},{"category":"section","location":"devdocs/architecture/#Buffer-ownership:-the-five-sub-structs","page":"Architecture","text":"The compiled buffers are grouped by kind of information into five sub-structs, while the derived scalars stay flat on the configuration:\n\nSub-struct Holds Backend-specific growth\ncells cell-list integer bookkeeping (hash, sort permutation, per-cell ranges, counting-sort workspace) the neighbour map — nothing on the CPU, a device half-shell map on the GPU\nparticles the 3 times N wrapped/sorted position and force buffers host↔device staging buffers — nothing on the CPU\ngrid the real-space force-density and fluid-velocity fields —\nsolver the Fourier-space field, the wavevectors, and the FFT plans cuFFT plans replace FFTW plans\nstencil per-particle stencil workspace unused by the GPU kernels (block-local shared memory replaces it)\n\nEach sub-struct is parameterized by its storage type, so the backend is a type-parameter swap: a configuration holding device arrays selects device kernels through ordinary dispatch, and no backend flag is stored or tested at run time. A backend can carry fields the other does not need (the neighbour map, the staging buffers) as extra type parameters holding nothing on the backend that does not use them.","title":"Buffer ownership: the five sub-structs"},{"category":"section","location":"devdocs/architecture/#The-assembly-seam","page":"Architecture","text":"The constructor runs three phases: validate the parameters, derive the backend-independent scalars and lookup tables, then assemble the backend buffers and plans. Only the third phase is backend-specific. The CPU assembly lives in src; the GPU assembly is supplied by the package extension through a method on a src-declared stub whose only src method throws — see the GPU architecture page for how that keeps src free of CUDA and the constructor fully inferred on the CPU path.","title":"The assembly seam"},{"category":"section","location":"devdocs/architecture/#Step-functions-and-function-barrier-kernels","page":"Architecture","text":"Each pipeline step is a public-by-convention wrapper (reachable qualified, e.g. ForceCouplingMethod.spread_forces!, but deliberately not exported — exporting the decomposition would leak it) that reads the configuration and delegates to a function-barrier kernel taking naked buffers and scalars. The barrier gives the compiler a concrete-typed call to specialise and lets each kernel be exercised in isolation by the tests. The backend split happens at these kernels: device methods dispatch on the device buffer types, so the wrappers — and mobility! itself — are identical source for both backends.","title":"Step functions and function-barrier kernels"},{"category":"section","location":"devdocs/architecture/#Storage-choices","page":"Architecture","text":"Grid fields (force_density, fluid_velocity, fluid_hat) are StructArrays of SVector{3} over three contiguous component arrays: struct-of-arrays memory for stride-1 per-component loops, with a per-grid-point SVector{3, T} read for consumers. The spread kernel destructures the components and writes plain arrays.\nParticle data are column-major 3 times N matrices, so one particle's three components are contiguous.\nCell indexing is Int32 throughout; realistic cell and particle counts fit comfortably.\nThe stencil scratch (per-axis Gaussian weights, squared distances, wrapped indices) is shared between the spread and the interpolation through one fill routine — the sharing is what makes interpolation the exact discrete adjoint of spreading (see why the uniform h³ weight). It is overwritten per particle and carries no between-call invariant.\nThe counting sort is mathcalO(N + m_x m_y m_z) and produces the per-cell ranges as a byproduct of its prefix sum, with no separate boundary scan; empty cells get a well-defined empty range.","title":"Storage choices"},{"category":"section","location":"devdocs/architecture/#Hot-path-contracts","page":"Architecture","text":"Three guarantees are pinned by the test suite (test/api/ and test/hygiene/), and changes must keep them green:\n\nZero allocations on mobility! and both mul! forms (BenchmarkTools.@ballocated == 0), scoped to fft_threads = 1 (threaded FFTW plans spawn tasks, which allocate).\nInference (Test.@inferred) on every public hot-path function, and JET's @test_opt pinning the hot path free of runtime dispatch.\nNo mutation of caller inputs: positions are folded into the configuration-owned wrap buffer, never in the caller's Y; mobility! writes V in the caller's original particle order.","title":"Hot-path contracts"},{"category":"section","location":"devdocs/architecture/#Internal-reference","page":"Architecture","text":"The step wrappers and buffer sub-structs, in pipeline order:","title":"Internal reference"},{"category":"function","location":"devdocs/architecture/#ForceCouplingMethod.wrap_positions!","page":"Architecture","text":"wrap_positions!(destination, source, L) -> destination\nwrap_positions!(Y, L) -> Y\n\nFold each column of the 3xN position matrix source into the periodic domain [0, L_i) for each axis i, writing the result into destination. A two-argument form  of the same function folds Y in place.\n\nArguments\n\ndestination::AbstractMatrix{T}: the 3xN destination matrix (may alias source).\nsource::AbstractMatrix{T}: the 3xN source positions.\nY::AbstractMatrix{T}: in the two-argument form, the matrix folded in place.\nL::NTuple{3, T}: the periodic box lengths.\n\nReturns\n\ndestination (or Y): the same matrix, with every column in [0, L_i).\n\nNotes\n\nThe floating point roundoff corner case where mod(y, L_i) rounds to exactly L_i is left to the downstream cell-index clamp in _assign_cells_kernel!.\n\n\n\n\n\n","title":"ForceCouplingMethod.wrap_positions!"},{"category":"function","location":"devdocs/architecture/#ForceCouplingMethod.assign_cells!","page":"Architecture","text":"assign_cells!(config, Y) -> config.cells.cell_hash\n\nStep 1 of the Fast FCM algorithm (Su & Keaveny 2024, §4). Write each particle's cell index into config.cells.cell_hash. The hash linearises the 3-D cell coordinate with x fastest and z slowest.\n\nArguments\n\nconfig::FFCMConfig{T}: the compiled configuration; config.cells.cell_hash is overwritten.\nY::AbstractMatrix{T}: the 3xN positions, already folded into the canonical domain by wrap_positions!.\n\nReturns\n\nconfig.cells.cell_hash: the per-particle 0-based cell indices.\n\n\n\n\n\n","title":"ForceCouplingMethod.assign_cells!"},{"category":"function","location":"devdocs/architecture/#ForceCouplingMethod.sort_particles_by_cell!","page":"Architecture","text":"sort_particles_by_cell!(config, Y, F) -> config\n\nStep 2 of the Fast FCM algorithm (Su & Keaveny 2024, §4). Counting-sort the particles by their cell hash and gather their positions and forces into that sorted order, so that particles sharing a cell are contiguous in memory.\n\nOn return, config holds:\n\nconfig.cells.original_index — sorted slot s to original particle index.\nconfig.cells.cell_start[c+1] : config.cells.cell_end[c+1] — 1-based inclusive range of sorted slots occupied by cell c (empty range if the cell is empty).\nconfig.particles.Y_sorted, config.particles.F_sorted — Y and F in sorted order.\n\nArguments\n\nconfig::FFCMConfig{T}: the compiled configuration; the sort outputs are written into its buffers. Assumes config.cells.cell_hash is current, i.e. assign_cells!(config, Y) ran since Y last changed.\nY::AbstractMatrix{T}, F::AbstractMatrix{T}: the 3xN positions and forces to gather into sorted order.\n\nReturns\n\nconfig: the same configuration, with the sort outputs populated.\n\n\n\n\n\n","title":"ForceCouplingMethod.sort_particles_by_cell!"},{"category":"function","location":"devdocs/architecture/#ForceCouplingMethod.spread_forces!","page":"Architecture","text":"spread_forces!(config) -> config\n\nStep 3 of the Fast FCM algorithm (Su & Keaveny 2024, §4; the spreading operator of §3 equation (25)). Evaluate the spread force density J̃†[F](x_g) = Σₙ Fₙ Δ̃ₙ(x_g; Σ) on every grid point x_g, writing the result into config.grid.force_density (zeroed at the start of the call).\n\nArguments\n\nconfig::FFCMConfig{T}: the compiled configuration. Reads config.particles.Y_sorted and config.particles.F_sorted (populated by sort_particles_by_cell!).\n\nReturns\n\nconfig: the same configuration, with config.grid.force_density overwritten by the spread force density.\n\n\n\n\n\n","title":"ForceCouplingMethod.spread_forces!"},{"category":"function","location":"devdocs/architecture/#ForceCouplingMethod.stokes_solve!","page":"Architecture","text":"stokes_solve!(config) -> config\n\nStep 4 of the Fast FCM algorithm (Su & Keaveny 2024, §4; the Fourier-space Stokes inversion of §3, equations (32)-(33)). Apply the inverse Stokes operator L^{-1} to the spread force field in config.grid.force_density and write the resulting fluid velocity field to config.grid.fluid_velocity: a forward FFT of each force_density component into fluid_hat, the per-mode projection (I - k̂⊗k̂)/(μ k²) / M in place on fluid_hat with the k = 0 mode zeroed (the mean-flow gauge fix), and an inverse FFT into fluid_velocity. The 1/M factor compensates the unnormalised FFTW round-trip.\n\nArguments\n\nconfig::FFCMConfig{T}: the compiled configuration. Reads config.grid.force_density (populated by spread_forces!); writes config.grid.fluid_velocity and overwrites config.solver.fluid_hat.\n\nReturns\n\nconfig: the same configuration, with config.grid.fluid_velocity holding the Stokes velocity field.\n\n\n\n\n\n","title":"ForceCouplingMethod.stokes_solve!"},{"category":"function","location":"devdocs/architecture/#ForceCouplingMethod.interpolate_velocities!","page":"Architecture","text":"interpolate_velocities!(V, config) -> V\n\nStep 5 of the Fast FCM algorithm (Su & Keaveny 2024, §4; the interpolation operator of §3 equation (26)). Interpolate the fluid velocity field config.grid.fluid_velocity (output of stokes_solve!) to each particle position, evaluating the modified-kernel volume average Ṽ_n = ∫ u(x) Δ̃_n(x; Σ) dx by the trapezoidal rule over the same M_G³ stencil as the spread, with weight h³.\n\nThe scatter-back via original_index is the inverse of step 2's gather, which makes interpolation the exact discrete adjoint of spread_forces! and the assembled mobility operator symmetric positive-definite.\n\nArguments\n\nV::AbstractMatrix{T}: a caller-owned 3xN matrix, overwritten with the particle velocities in the caller's original particle order.\nconfig::FFCMConfig{T}: the compiled configuration. Reads config.grid.fluid_velocity, config.particles.Y_sorted, and config.cells.original_index(populated by sort_particles_by_cell!).\n\nReturns\n\nV: the same matrix, holding the interpolated particle velocities.\n\n\n\n\n\n","title":"ForceCouplingMethod.interpolate_velocities!"},{"category":"function","location":"devdocs/architecture/#ForceCouplingMethod.correct_velocities!","page":"Architecture","text":"correct_velocities!(V, config) -> V\n\nStep 6 of the Fast FCM algorithm (Su & Keaveny 2024, §4). Add the real-space pairwise correction M^VF - M̃^VF (paper §3 equation (31)) and the per-particle self term (paper Appendix B, equation (B.1)) to the interpolated velocities V, the same 3xN buffer interpolate_velocities! wrote, in the caller's original particle order. The correction is added in place (V[:, n] += ΔV_n), realising M = M̃ + (M - M̃).\n\nEach particle gathers the correction from neighbours within config.R_c over the 27 surrounding cells, applying ΔV_n += isotropic_coefficient⋅F_m + parallel_coefficient⋅(x⋅F_m)⋅x with the two pair scalars of _correction_scalars and the closed-form self term. The result completes the σ-regularised mobility, independent of Σ.\n\nArguments\n\nV::AbstractMatrix{T}: the 3xN velocity matrix to correct in place (the output of interpolate_velocities!, in original particle order).\nconfig::FFCMConfig{T}: the compiled configuration. Reads config.particles.Y_sorted, config.particles.F_sorted, the cell list (config.cells.cell_start / config.cells.cell_end), and config.cells.original_index (populated by sort_particles_by_cell!).\n\nReturns\n\nV: the same matrix, with the pairwise + self correction added.\n\n\n\n\n\n","title":"ForceCouplingMethod.correct_velocities!"},{"category":"type","location":"devdocs/architecture/#ForceCouplingMethod.CellBuffers","page":"Architecture","text":"Cell-list bookkeeping for the pairwise correction (paper §4): the per-particle cell hash, the sort permutation, the per-cell occupancy ranges with the counting-sort workspace, and the GPU neighbour map. The first five buffers are integer-indexed.\n\nThe storage type IntVector is Vector{Int32} on the CPU backend and the device vector type on the GPU backend, so the backend is a type-parameter swap. NeighborMap is the single backend-divergent field: Nothing on the CPU (whose correction computes neighbours on the fly) and the device half-shell map on the GPU.\n\nFields\n\ncell_hash::IntVector: per-particle 0-based linear cell index (length N).\noriginal_index::IntVector: sorted slot to original particle index (length N).\ncell_start::IntVector, cell_end::IntVector: 1-based inclusive sorted-slot range of each cell (length prod(num_cells)).\ncounting_sort_scratch::IntVector: per-cell counting-sort workspace (length prod(num_cells)).\nneighbor_map::NeighborMap: nothing on the CPU; on the GPU the device vector of 13 forward half-shell neighbour cells per cell (length 13 * prod(num_cells)), built by _build_neighbor_map and consumed by the pairwise correction.\n\n\n\n\n\n","title":"ForceCouplingMethod.CellBuffers"},{"category":"type","location":"devdocs/architecture/#ForceCouplingMethod.ParticleBuffers","page":"Architecture","text":"Particle-indexed 3xN work buffers the cell-list pipeline folds and reorders the caller's positions and forces into (paper §4), kept distinct from the integer cell-list bookkeeping in CellBuffers.\n\nThe storage type Mat is Matrix{T} on the CPU backend and the device matrix type on the GPU backend. Staging is the backend-divergent type of the host↔device staging buffers: Nothing on the CPU (which consumes the caller's host arrays directly) and the device matrix type on the GPU, where mobility! uploads the caller's positions/forces into Y_input/F_input and downloads the velocities from V_output, hiding the host↔device traffic.\n\nFields\n\nY_wrapped::Mat: caller positions folded into the periodic box (so the caller's Y is never mutated).\nY_sorted::Mat, F_sorted::Mat: positions and forces in cell-sorted order.\nY_input::Staging, F_input::Staging: nothing on the CPU; on the GPU the device-resident copies of the caller's raw 3xN positions and forces.\nV_output::Staging: nothing on the CPU; on the GPU the device-resident 3xN velocity output, copied back into the caller's V after the pipeline.\n\n\n\n\n\n","title":"ForceCouplingMethod.ParticleBuffers"},{"category":"type","location":"devdocs/architecture/#ForceCouplingMethod.GridBuffers","page":"Architecture","text":"The real-space grid fields of the FCM solve (paper §3): the spread force density and the interpolated fluid velocity, both (M_x, M_y, M_z) fields of 3-vectors.\n\nThe storage type GridField is a StructArray{SVector{3, T}} whose component arrays are Array{T, 3} on the CPU backend and the device array type on the GPU backend.\n\nFields\n\nforce_density::GridField: spread force density J̃†[F] (output of spread_forces!).\nfluid_velocity::GridField: Stokes velocity field (output of stokes_solve!).\n\n\n\n\n\n","title":"ForceCouplingMethod.GridBuffers"},{"category":"type","location":"devdocs/architecture/#ForceCouplingMethod.SolverState","page":"Architecture","text":"Fourier-space state of the spectral Stokes solve (paper §3): the half-complex force/velocity field, the per-axis wavevectors, and the cached FFT plans. Named for the solve rather than the transform so a future non-spectral Stokes solver can reuse the boundary.\n\nThe component storage swaps with the backend; the FFT plans are FFTW plans on the CPU backend and cuFFT plans on the GPU backend.\n\nFields\n\nfluid_hat::SpectralField: half-complex (M_x÷2+1, M_y, M_z) field reused as the forward-transform output and the Stokes-solved spectrum.\nk_x::RealVector, k_y::RealVector, k_z::RealVector: per-axis Fourier wavevectors in FFTW layout.\nforward_fourier_transform::FwdTransform, inverse_fourier_transform::InvTransform: the real-to-complex and complex-to-real plans.\n\n\n\n\n\n","title":"ForceCouplingMethod.SolverState"},{"category":"type","location":"devdocs/architecture/#ForceCouplingMethod.StencilBuffers","page":"Architecture","text":"Per-particle stencil workspace shared by spreading and interpolation (paper §5): the separable Gaussian weights, the squared grid-point distances, and the wrapped grid indices over the M_G cubic support. These are recomputed for each particle and carry no state between mobility! calls.\n\nThe storage type swaps with the backend.\n\nFields\n\nstencil_gaussian::StencilField: separable Gaussian stencil weights (length M_G).\nstencil_r²::StencilField: squared distances to the stencil grid points (length M_G).\nstencil_index::StencilIndexField: periodic-wrapped grid indices (length M_G).\n\n\n\n\n\n","title":"ForceCouplingMethod.StencilBuffers"},{"category":"section","location":"devdocs/gpu-architecture/#GPU-architecture","page":"GPU architecture","text":"This page documents how the CUDA backend is organised: the extension boundary, the device kernel architecture of each pipeline step, the host↔device staging that hides device arrays from callers, and the parity contract binding the two backends. The user-facing story is on the GPU acceleration page.\n\nThe GPU backend's kernel architecture follows the paper authors' reference CUDA implementation, cuFCM, against which this package was validated for numerical agreement and throughput; the measured reference baseline is kept in benchmark/cufcm-baseline.md in the repository. Performance-motivated deviations from that validated architecture are benchmark-gated, and their outcomes are recorded on this page as they land.","title":"GPU architecture"},{"category":"section","location":"devdocs/gpu-architecture/#Backend-selection-is-a-construction-time-type-choice","page":"GPU architecture","text":"The configuration owns every per-call buffer, and buffers must live where the kernels run — host memory for the CPU backend, device memory for the GPU backend. The backend is therefore fixed when the buffers are allocated, at construction, and recorded in the configuration's type through the storage-type parameters of its sub-structs. The step functions dispatch on their buffer arguments with no runtime branch, and mobility! is identical source for both backends.\n\nThe user expresses the choice as the single intent flag gpu_acceleration::Bool rather than naming a device array type: the flag hides all CUDA vocabulary from callers, routes the cold-path buffer assembly, and is not stored.","title":"Backend selection is a construction-time type choice"},{"category":"section","location":"devdocs/gpu-architecture/#The-extension-boundary","page":"GPU architecture","text":"CUDA appears only in the ext/ForceCouplingMethodCUDAExt package extension, never in src. The extension supplies the GPU buffer/plan assembly by adding a method to a function declared in src whose only src method throws an ArgumentError naming the requirement. That fallback has a useful inference consequence: when CUDA is not loaded, the GPU branch of the constructor infers to Union{}, so the constructor remains fully inferred on the default CPU path.\n\nGPU construction launches no kernel: it allocates device buffers and creates the cuFFT plans, neither of which JIT-compiles device code. The buffers are left unzeroed — every buffer is written before it is read on a mobility! call — which keeps construction independent of the device's kernel-JIT path (the CPU backend zeros its grids only because FFTW's measuring planner dirties them).","title":"The extension boundary"},{"category":"section","location":"devdocs/gpu-architecture/#Kernel-architecture-by-step","page":"GPU architecture","text":"The device kernels are hand-written @cuda kernels, added as device methods of the same function-barrier kernels the CPU defines, dispatched on the device buffer types.\n\nStep 1 — wrap, hash, cell list. The wrap and hash are grid-stride kernels, one thread per particle, running the same fold / floor / clamp / linearise arithmetic as the CPU (the hash is integer arithmetic on the same scalars, so it is bit-identical). The cell list is a parallel counting sort: an atomic-increment histogram, a prefix scan (accumulate!) to the per-cell ranges, and an atomic-cursor scatter of the permutation. The per-cell ranges depend only on the counts and match the CPU's exactly; the atomic scatter is not stable, so the intra-cell order of original_index (and hence the column order of the sorted buffers) is unspecified — numerically immaterial, since every downstream sum over a cell is order-independent to round-off. The half-shell neighbour map consumed by the correction (13 forward neighbours per cell) is geometry-only, so it is built once on the host at construction and copied to the device; it is the cells sub-struct's backend-divergent field.\n\nStep 3 — force spreading. One block per particle (block-stride beyond the launch grid), 32 threads per block — a single warp. The block cooperatively stages the separable per-axis stencil — 1-D Gaussian weights, squared distances, wrapped 1-based grid indices — in dynamic shared memory, which replaces the CPU's global stencil scratch (the GPU path leaves the stencil sub-struct unused). The threads then sweep the M_G^3 support, each forming the modified-kernel weight (a_0 + a_2 r^2) g_x g_y g_z and atomically adding the monopole force contribution to the three grid components. The polynomial coefficients and Gaussian normalisation come from the same host-side helper the CPU kernel uses, and the anchor and periodic wrap follow the CPU conventions, so only the atomic accumulation order differs from the CPU result.\n\nStep 4 — Stokes solve. stokes_solve! needs no GPU-specific entry: its forward and inverse transforms go through the AbstractFFTs mul! interface, which dispatches to the cuFFT plans the assembly built. The per-mode projection gets a device method: one thread per Fourier mode over the half-spectrum, grid-stride, each thread reading k_xi_x k_yi_y k_zi_z from the per-axis wavenumber vectors the assembly left resident on the device — so the two backends see bit-identical wavevectors — and applying the same normalised projector with the boldsymbolk = boldsymbol0 gauge fix as a guarded write of zero. The whole step stays device-resident: forward cuFFT → in-place projection → inverse cuFFT, with no host round trip.\n\nStep 5 — interpolation / gather. The gather mirror of the spread: one block per particle, 32 threads, the same shared-memory stencil staging. Each thread accumulates a partial kernel-weighted velocity over its slice of the M_G^3 support; because the 32-thread block is a single warp, the partials are summed with a warp-shuffle reduction — no shared scratch and no atomics, since the gather only reads the grid and each particle writes its own output column. Lane 0 applies the h^3 quadrature factor once and writes V[:, original_index[np]], folding the unsort into the write exactly as the CPU kernel does.\n\nStep 6 — pairwise correction. One thread per sorted particle, grid-stride. The thread recomputes its particle's cell from the wrapped position (the same hash as step 1), seeds its accumulator with the folded self term deltaboldsymbolF_i, sweeps every other particle in its own cell accumulating the correction to itself only, then sweeps the 13 forward neighbour cells from the device neighbour map: each in-range pair is visited once, and because the correction tensor is symmetric, the same pair scalars give both the correction to i (kept in registers) and the correction to j (atomically added to j's original-order column). The accumulated self-plus-own correction is atomically added to V[:, original_index[i]]. The pair scalars and the minimum image are the CPU's own helpers — the minimum image is exactly antisymmetric, so each pair's scalars are bit-identical across backends and only the atomic summation order differs.\n\nThis step's dispatch seam is wider than the others: the shared kernel signature carries the neighbour map and the inverse cell size, and dispatch is on the neighbour-map argument — nothing selects the CPU method (an on-the-fly 27-cell gather that ignores both extra arguments), a device vector selects the GPU method.","title":"Kernel architecture by step"},{"category":"section","location":"devdocs/gpu-architecture/#The-hostdevice-boundary","page":"GPU architecture","text":"The caller of mobility! passes ordinary host 3 times N arrays for both backends. The assembled operator stages them across the boundary: mobility! opens with a stage call and closes with a retrieve call, both dispatched on the particles sub-struct storage type. On the CPU backend the stage returns the caller's arrays unchanged and the retrieve is a no-op — byte-for-byte the CPU-only behaviour, copy-free. On the GPU backend the stage copyto!s Y and F into device staging buffers and the retrieve copyto!s the device velocities back; everything between the two edges stays resident on the device. Because the seam dispatches like the step kernels, mobility! remains one source with no backend test in its body, and the FFCMMobility operator keeps its host scratch and drives a GPU configuration unchanged — the traffic is hidden one level below the linear-operator interface.\n\nThe GPU-backed particles sub-struct owns three device buffers for this (position and force uploads, velocity download); the CPU-backed one carries nothing in their place.","title":"The host↔device boundary"},{"category":"section","location":"devdocs/gpu-architecture/#The-parity-contract","page":"GPU architecture","text":"The GPU backend is correct when, for the same positions and forces, it reproduces the CPU backend's velocities to the documented tolerance (sqrtmathrmeps(T) relative, with the absolute floor for near-zero entries). Parity is asserted per pipeline step and end to end by test/cuda/, which runs wherever CUDA.functional() is true; each per-step test writes the step's inputs directly and identically on both backends, isolating it from the steps before it. Parity is to round-off rather than bit-exact because the atomic scatters and parallel reductions fix the result but not the floating-point summation order. The GPU hot path is additionally held to CUDA.@allocated == 0, mirroring the CPU allocation contract.","title":"The parity contract"},{"category":"section","location":"devdocs/gpu-architecture/#Deferred,-benchmark-gated-variants","page":"GPU architecture","text":"The initial port takes the validated reference architecture unchanged; each variant below is adopted only on a measured win against it, and outcomes are recorded here.\n\nA stable, deterministic mathcalO(N) radix sort in place of the atomic counting sort (restores intra-cell determinism; relevant if reproducibility is required or atomic contention bottlenecks).\nAn allocation-free prefix scan for large cell counts (accumulate! allocates a transient aggregate buffer on its multi-block path).\nBuilding the correction's neighbour shell on the fly instead of the precomputed map (would drop the backend-divergent cells field).\nLaunch-shape tuning: 32-versus-larger thread blocks, grid-stride mappings, and no-shared-memory or thread-per-particle spread/gather variants.\nA shared spread↔gather device stencil-fill helper (the precompute is currently duplicated in the two kernels; the CPU shares one routine), plus naming alignment of the two kernels' launch helpers.\nA 27-cell gather correction (no atomics, like the CPU) versus the half-shell atomic dual write; a separate self-term kernel versus the folded self term.\nCaching the position upload for operators with fixed positions across a solve (FFCMMobility re-uploads Y every mul!); a zero-copy fast path when the caller already holds device arrays; staging-buffer minimisation.\n\nMeasured so far (RTX 2080 Ti, driver 460.27.04): the port at the reference architecture reached 111.7 % of the reference implementation's hashing+compute throughput at N approx 19 times 10^5, 256³ grid, Float32 — with per-call staging included — and the FFT-library-vintage explanation for that edge was tested and refuted (the reference binary ran no faster under the newer cuFFT; details in benchmark/cufcm-baseline.md).","title":"Deferred, benchmark-gated variants"},{"category":"section","location":"gpu/#GPU-acceleration","page":"GPU acceleration","text":"The CUDA backend runs the whole pipeline — cell list, spreading, FFT Stokes solve, interpolation, and pairwise correction — resident on an NVIDIA GPU. It lives in a package extension that loads automatically when CUDA.jl is present, so the package has no GPU dependency unless you ask for one.","title":"GPU acceleration"},{"category":"section","location":"gpu/#Usage","page":"GPU acceleration","text":"Load CUDA alongside ForceCouplingMethod and pass gpu_acceleration = true at construction:\n\nusing CUDA\nusing ForceCouplingMethod\n\nconfig = FFCMConfig(;\n    L = (250.0f0, 250.0f0, 250.0f0),\n    num_grid_points = Int32.((256, 256, 256)),\n    kernel_widths_ratio = 2.0f0,\n    M_G = 10,\n    R_c = 5.6419f0,\n    N = 186_510,\n    viscosity = 1.0f0,\n    gpu_acceleration = true,\n)\n\nV = zeros(Float32, 3, N)\nmobility!(V, config, Y, F)   # Y, F, V are ordinary host arrays\n\nThe backend is chosen once, at construction, and encoded in the configuration's storage types — it is not stored as a flag, and mobility! contains no backend branch. Everything else about the API is unchanged: the same FFCMConfig keywords, the same mobility! call, and the same FFCMMobility operator, which drives a GPU configuration through mul! exactly as it drives a CPU one.","title":"Usage"},{"category":"section","location":"gpu/#Driver-compatibility","page":"GPU acceleration","text":"The GPU backend depends on CUDA.jl, whose supported NVIDIA drivers are independent of this package. CUDA.functional() returns true when the installed CUDA.jl is compatible with the machine's driver and the backend is ready to run. ForceCouplingMethod supports CUDA.jl 5.8–6.2, and the package manager installs the newest compatible release by default; on older, CUDA 11-era drivers that newest series reports CUDA.functional() == false and mobility! cannot run on the device.\n\nIf using CUDA; CUDA.functional() returns false and updating the driver is not an option, install an older CUDA.jl within the supported range and pin it in your Manifest.toml:\n\npkg> add CUDA@5.8.5\npkg> pin CUDA\n\nSee the CUDA.jl documentation for the driver-to-version matrix.","title":"Driver compatibility"},{"category":"section","location":"gpu/#What-to-expect","page":"GPU acceleration","text":"Host arrays in, host arrays out. Y, F, and V are ordinary 3 times N host matrices for both backends. A GPU configuration uploads the inputs and downloads the velocities internally; you never allocate or handle a device array.\nCUDA must be loaded and functional. gpu_acceleration = true without using CUDA (or without a working CUDA device) throws an ArgumentError naming the requirement.\nFloat32 is the intended GPU precision. Float64 is permitted but emits a warning at construction: consumer NVIDIA cards run double precision at a small fraction of single-precision throughput.\nCPU↔GPU agreement. For the same positions and forces, the GPU backend reproduces the CPU backend's velocities to the documented round-off tolerance (sqrtmathrmeps(T) relative). The two backends differ only in floating-point summation order (GPU kernels accumulate atomically or in parallel), never in the algorithm. The test suite asserts this parity per pipeline step and end to end whenever a CUDA device is available.\nThe hot path allocates nothing on the device, mirroring the CPU allocation-free guarantee.\nfft_threads is a CPU concept (it configures the FFTW plans) and is ignored by the GPU backend, whose transforms run through cuFFT.\n\nHow the device kernels are organised — launch shapes, shared-memory staging, the host↔device boundary — is documented in the GPU architecture page of the developer documentation.","title":"What to expect"},{"category":"section","location":"method/cell-lists/#Cell-lists","page":"Cell lists","text":"The pairwise correction must find, for every particle, all neighbours within the cutoff R_c. Steps 1–2 of the pipeline (Su & Keaveny 2024, §4) make that search mathcalO(N): the periodic box Omega = 0 L_x) times 0 L_y) times 0 L_z) is partitioned into rectangular cells sized so that every neighbour within R_c of a particle lies in the particle's own cell or one of its 26 immediate neighbours, and the particle data are sorted so each cell's particles occupy contiguous memory.","title":"Cell lists"},{"category":"section","location":"method/cell-lists/#Cell-geometry-and-the-covering-guarantee","page":"Cell lists","text":"The number of cells per axis is\n\nm_i = maxleft(leftlfloor fracL_iR_c rightrfloor 3right)\nqquad (text4 Step 1)\n\nWhy the 3 times 3 times 3 block suffices splits into two regimes, depending on which term of the max is active.\n\nThe box is at least three cutoffs wide on axis i (L_iR_c geq 3). Then m_i = lfloor L_iR_c rfloor leq L_iR_c, so each cell is at least as wide as the cutoff, L_im_i geq R_c. Any particle within R_c of a given particle differs by at most one cell width per axis, so its cell coordinate differs by at most one, and the 3 times 3 times 3 block contains the whole R_c-ball.\n\nThe box is narrower than three cutoffs on axis i (2 leq L_iR_c  3, the range still permitted by the correction's precondition R_c leq min(L)2). The floor clamps to m_i = 3 and a cell may be narrower than the cutoff, but coverage holds for a different reason: with only three cells on the axis, the neighbour offsets -1 0 +1 bmod 3 = 0 1 2 span every cell, so the block sweeps the whole axis and no neighbour can be missed.\n\nIn both regimes the floor m_i geq 3 also guarantees that, under periodicity, the 26 neighbours are distinct cells, so none is double-counted (with m_i = 2 the offsets -1 +1 bmod 2 would collide).","title":"Cell geometry and the covering guarantee"},{"category":"section","location":"method/cell-lists/#Hashing-a-position-to-a-cell","page":"Cell lists","text":"Positions are first folded into 0 L_i) by Y_ni mapsto operatornamemod(Y_ni L_i). This is the canonical wrap point for the whole pipeline: after it, every later step may assume boldsymbolY_n in Omega. A wrapped position then maps to the cell coordinates and linear cell index\n\nc_i = minleft(leftlfloor fracY_nitextcell size_i rightrfloor m_i - 1right)\nqquad\ntextcell index = c_x + (c_y + c_z m_y) m_x\nqquad (text4 equation (68))\n\nwith x laid out fastest and z slowest. The min(cdot m_i - 1) clamp guards one floating-point corner: if Y_ni is just below L_i and the division rounds up to exactly m_i, the floor would land one past the last cell; the clamp folds it back. The boundary behaviour is therefore: Y_ni = 0 maps to cell coordinate 0, and Y_ni = L_i — reachable only by floating-point round-off — maps to m_i - 1.","title":"Hashing a position to a cell"},{"category":"section","location":"method/cell-lists/#Sorting-particles-by-cell","page":"Cell lists","text":"With every particle hashed, the particle data are ordered by cell with a counting sort on the integer key. Because the keys are bounded by the cell count, no comparison sort is needed, and the per-cell index ranges fall out of the sort itself:\n\nHistogram. Count how many particles fall in each cell.\nExclusive prefix sum. Turn the counts into the 1-based inclusive range textcell startc  textcell endc of sorted slots cell c will occupy. An empty cell gets textcell endc = textcell startc - 1 — a well-defined empty range for any occupancy, so the correction can iterate any cell without a guard.\nStable scatter. Walk the particles in ascending original index and place each in the next free slot of its cell, recording the permutation textoriginal indexs = n from sorted slot s to original particle n.\n\nA final gather materialises the sorted data, boldsymbolY^textsorted_s = boldsymbolY_textoriginal indexs and likewise for the forces, so particles sharing a cell occupy contiguous columns. That contiguity is the memory locality the later steps rely on: the spread and interpolation touch overlapping grid patches for consecutive particles, and the pairwise correction walks a cell by its first and last slot.","title":"Sorting particles by cell"},{"category":"section","location":"#ForceCouplingMethod.jl","page":"Home","text":"Julia implementation of the Fast Force-Coupling Method (FFCM) of Su & Keaveny (2024), Accelerating the force-coupling method for hydrodynamic interactions in periodic domains, J. Comput. Phys. 510, 113060, for hydrodynamic interactions between rigid spheres in a triply-periodic Stokes flow.\n\nFFCM is a numerical approximation to the mobility tensor of a collection of identical spherical rigid particles suspended in a fluid that obeys the Stokes equation with periodic boundary conditions. Given N spherical particles of radius a = 1 located at boldsymbolY_n with forces boldsymbolF_n acting on them, FFCM computes their resulting velocities boldsymbolV_n considering hydrodynamic interactions.","title":"ForceCouplingMethod.jl"},{"category":"section","location":"#Installation","page":"Home","text":"ForceCouplingMethod.jl is registered in the Julia General registry: from the Julia REPL, enter package mode with ] and run\n\npkg> add ForceCouplingMethod","title":"Installation"},{"category":"section","location":"#Usage","page":"Home","text":"The package provides three objects for plug-and-play usage of the method. FFCMConfig provides the setup for the problem, where the physics (domain size, number of particles, viscosity) and numerical parameters (spectral method grid, local stencil size, method parameters) can be set.\n\nFFCMMobility is the actual mobility operator, and mobility! is a mutating function that acts on its first argument, populating it with the velocities given by the arguments (both interfaces share the same backend).\n\nusing ForceCouplingMethod\n\nT = Float32  # or Float64\n\nL_x = T(10)  # Set \\Omega = [0, 10)^3\nL_y = T(10)\nL_z = T(10)\n\nviscosity = T(1)  # Fluid dynamic viscosity\n\nR_c = T(1)  # Cutoff radius\nkernel_widths_ratio = T(2)  # FFCM splitting parameter\n\nY = T[9.3 4.1 -0.4 4.0; 4.0 4.2 4.0 9.9; 4.0 4.0 4.0 4.0]  # Positions 3xN\nF = T[0.5 -0.3 0.2 0.1; -0.1 0.4 0.0 0.3; 0.2 0.1 0.7 -0.5]  # Forces 3xN\nN = size(Y, 2)  # Number of particles\n\nnum_points = Int32(16)  # Grid discretisation\nM_G = 8  # Stencil size\n\nconfig = FFCMConfig(\n    L = (L_x, L_y, L_z),\n    R_c = R_c,\n    N = N,\n    kernel_widths_ratio = kernel_widths_ratio,\n    viscosity = viscosity,\n    num_grid_points = (num_points, num_points, num_points),\n    M_G = M_G,\n)\n\nV = zeros(T, 3, N)  # Target array for velocities, size 3xN of type T\nmobility!(V, config, Y, F)  # Use FFCM as a function\n\nM = FFCMMobility(config, Y)  # Mobility operator\nV = M * F  # Alternatively, use the method as a linear operator\n\nThe working precision is inferred from the element type of the domain lengths L; it can also be set explicitly with FFCMConfig{T}(...).\n\nThe first mobility! (or M * F) call after loading the package incurs Julia's just-in-time compilation. For timing or long solves, make one warmup call on the configuration first; subsequent calls run at full speed.\n\nOn a machine with an NVIDIA GPU, constructing with gpu_acceleration = true (after using CUDA) runs the whole pipeline on the device with the same API — see GPU acceleration.","title":"Usage"},{"category":"section","location":"#Reading-this-documentation","page":"Home","text":"The method pages explain the algorithm — the fast-FCM splitting and each pipeline step — citing Su & Keaveny (2024) by section and equation; Notation maps the paper's symbols to code identifiers.\nGPU acceleration and Performance tips cover practical use.\nThe API reference documents the three exported names; the developer documentation documents the internals.","title":"Reading this documentation"},{"category":"section","location":"notation/#Notation","page":"Notation","text":"A quick reference mapping the symbols of Su & Keaveny (2024) to the code identifiers used throughout this documentation. Each method page introduces only the symbols new to its step; the symbols shared across steps are collected here.\n\nnote: Note\nPaper section and equation numbers cited throughout this documentation have been verified against the published article (Su & Keaveny 2024, J. Comput. Phys. 510, 113060). The one exception is the operatornameerf-argument typo in its equation (31), documented on the pairwise correction page.","title":"Notation"},{"category":"section","location":"notation/#Index-conventions","page":"Notation","text":"i in x y z equiv 1 2 3 is a Cartesian axis index.\nn m in 1 dots N are particle indices.\nParticle data live in column-major 3 times N matrices, so Y[i, n] is component i of particle n (the code indexes from 1).\n(i_x i_y i_z) is a 1-based grid-point index on the spectral grid: the grid point is boldsymbolx_g = bigl((i_x - 1)h (i_y - 1)h (i_z - 1)hbigr). The same letters index the Fourier grid, where they address the wavenumbers k_x[i_x], k_y[i_y], k_z[i_z].","title":"Index conventions"},{"category":"section","location":"notation/#Symbols","page":"Notation","text":"L = (L_x L_y L_z) — the lengths of the periodic box, so the domain is Omega = 0 L_x) times 0 L_y) times 0 L_z).\nN = N — the number of particles.\nY = (boldsymbolY_n)_n=1^N — the particle positions; boldsymbolY_n is the position of particle n.\nF = (boldsymbolF_n)_n=1^N — the forces on the particles.\nV = (boldsymbolV_n)_n=1^N — the particle velocities the operator returns.\nR_c = R_c — the cutoff radius of the pairwise correction.\nnum_cells = (m_x m_y m_z) — the number of cells per axis in the cell list.\ncell_size = (L_xm_x L_ym_y L_zm_z) — the cell extents per axis; inv_cell_size holds their reciprocals (precomputed for the hot path).\ncell_hash — cell_hash[n] is the linear cell index of particle n.\noriginal_index — the sort permutation, sorted slot mapsto original particle index (n = textoriginal indexs).\ncell_start, cell_end — the per-cell 1-based inclusive range of sorted slots; counting_sort_scratch is the counting-sort scratch (the per-cell histogram, then the per-cell write cursor).\na = a — the particle radius; a = 1 in the current implementation.\nσ = sigma — the FCM Gaussian width, sigma = asqrtpi.\nΣ = Sigma — the modified (fast-FCM) kernel width, with Sigma geq sigma.\nΣ_over_σ = Sigmasigma — the resolution control parameter (the kernel_widths_ratio keyword).\nμ = mu — the dynamic viscosity. The paper writes eta for this quantity.\nnum_grid_points = (M_x M_y M_z) — the grid points per axis of the spectral grid; the total is M = M_x M_y M_z.\nM_G = M_G — the per-axis width of the cubic M_G times M_G times M_G kernel stencil.\nh = h — the uniform grid spacing h = L_iM_i, equal across axes.\nk_x, k_y, k_z — the per-axis wavenumbers k_i = 2pi n_iL_i of the spectral grid, with n_i the signed Fourier index; together they form the wavenumber vector boldsymbolk.\nforce_density = boldsymbolf and fluid_velocity = boldsymbolu — the spread force density and the fluid velocity, sampled on the spectral grid.\nfluid_hat — the Fourier transform of force_density on entry to the Stokes solve, overwritten with that of fluid_velocity on exit.","title":"Symbols"},{"category":"section","location":"notation/#Operators-and-kernels","page":"Notation","text":"mathcalM^mathcalVmathcalF — the mobility operator, mapping the forces mathcalF at positions mathcalY to the velocities mathcalV they induce through the periodic Stokes flow.\ntildemathcalM^mathcalVmathcalF — the modified mobility, built from the width-Sigma kernel; the spectral steps compute this and the pairwise correction recovers mathcalM^mathcalVmathcalF.\ntildemathcalJ^dagger and tildemathcalJ — the spreading operator and its adjoint, interpolation.\nmathcalL^-1 — the inverse periodic Stokes operator.\nDelta_n(boldsymbolx w) — an isotropic Gaussian of width w centred at boldsymbolY_n.\ntildeDelta_n(boldsymbolx Sigma) — the modified (fast-FCM) kernel.\nDelta — the Laplacian. The paper writes nabla^2.\nboldsymbolG boldsymbolS boldsymbolQ boldsymbolT — the Stokeslet (Oseen tensor) and the operators of the FCM mobility expansion entering the real-space correction; their closed forms are on the pairwise correction page.\nA B delta — the two pair scalars of the correction tensor AboldsymbolI + Bboldsymbolx otimes boldsymbolx and the self-correction scalar; code isotropic_coefficient, parallel_coefficient, self_correction_term.","title":"Operators and kernels"}]
}
